# Engineering Design

## Architecture

The repository is a static, independently installable skill collection under `skills/<skill-name>/`. Each package may contain `SKILL.md`, `agents/openai.yaml`, and only the references, scripts, or eval assets required by its protocol. The four packages do not import one another at runtime.

The shared engineering model is:

```text
PRD / EDD (human-owned intent)
  → GitHub Issue (one independently deliverable Work Contract)
  → branch or worktree implementation
  → commits + one final delivery Pull Request (normally one per Issue)
  → PR CI and task-local review
  → authorized merge to default branch (accepted implementation reality)
  → Main CI
      → success: completion comment + close linked open Issue as completed
      → non-success: failure comment + keep linked Issue open
  → independent review-repo audit
  → high-confidence finding through create-issue
```

Each Issue must remain independently deliverable. One Issue normally has one final delivery PR, and multiple ordinary PRs must not share responsibility for completing it; if one final PR cannot reasonably deliver the outcome, intake must re-boundary the Issue before implementation. Ordinary planning, branch/worktree handling, commits, pushes, PR creation, CI repair, review, and authorized merge are host-agent and Git/GitHub capabilities. Issue finalization is a small repository workflow generated by `bootstrap-repo`, not a new skill or orchestration layer: PRs use exact `Refs #N` lines, merge never implies completion, and only Main CI success for the merged commit closes linked open Issues with `state_reason: completed`.

## Authority and portability

PRD and EDD semantics change only through explicit human resolution. Issues scope independently deliverable work; final delivery PRs preserve delivery evidence. A handoff and later audit must be able to reconstruct `Issue → final delivery PR → merge commit → default branch → Main CI → Issue state` from repository-visible artifacts and cannot depend on hidden reasoning, session state, or a particular Agent vendor. Closed Issues remain historical records and are never deleted by the finalizer.

`create-issue` uses an authenticated `gh` CLI session through a standard-library wrapper and fails closed on missing identity, authentication, duplicate checks, or review-finding eligibility. `.agents/skills/create-issue/` remains a byte-identical discovery mirror of the portable package.

## Validation and CI

The root validator checks the exact skill inventory, package structure, UTF-8/Python/JSON validity, mirror fidelity, retired-workflow absence, structured protocol manifests, and forbidden orchestration skills. Focused tests exercise bootstrap discovery and template rendering, Main CI Issue finalization with mocked GitHub APIs, Issue backend guards, cross-skill protocol invariants, and review fixture generation. CI runs the same deterministic commands on pushes and Pull Requests.

The generated `issue-finalize.yml` is separate from project CI. It listens for completion of the configured CI workflow, then gates execution to `workflow_run.event == 'push'` and the repository's runtime `default_branch`. It uses `repos.listPullRequestsAssociatedWithCommit` plus exact merge SHA/base checks, parses only standalone `Refs #N` lines, rejects Pull Requests returned through the Issues API, and uses a workflow-run/attempt marker to make comments idempotent. Its least-privilege token grants `contents: read`, `pull-requests: read`, and `issues: write`.

Generated caches, temporary evaluation worktrees, raw model logs, local paths, and credentials are excluded from version control.
