interface:
  display_name: "Repository Consistency Review"
  short_description: "Audit repository health with Issue/PR provenance"
  default_prompt: "Use $review-repo to independently audit this repository across PRD/EDD, Issues, PRs, accepted code, tests, CI, and docs."
