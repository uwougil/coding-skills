## Issue

Refs #<number>

<!--
`Refs #<number>` links this PR to an Issue without closing it.
One Issue is normally completed by one final delivery PR. If this PR cannot reasonably deliver the complete outcome, re-boundary the Issue before implementation instead of sharing completion responsibility across multiple ordinary PRs.
Do not use `Closes`, `Fixes`, or `Resolves`. Merge is not completion.
The Issue is completed only after the default-branch CI succeeds; automation then comments and closes it.
Agents that create PRs must preserve this lifecycle.
-->

## What changed

...

## Verification

...

## Contract impact

PRD: unchanged / decision required

EDD: unchanged / decision required
