interface:
  display_name: "Create Issue"
  short_description: "Compile accepted work into guarded GitHub Issues"
  default_prompt: "Use $create-issue to turn settled intent or an eligible review finding into a verified GitHub Issue Work Contract."
policy:
  allow_implicit_invocation: true
