interface:
  display_name: "Fix Bug"
  short_description: "Evidence-first bug diagnosis and regression-safe repair"
  default_prompt: "Use $fix-bug to execute the linked bug Issue: reproduce, diagnose, minimally repair, verify, and prepare PR-ready evidence."
policy:
  allow_implicit_invocation: true
