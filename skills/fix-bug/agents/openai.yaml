interface:
  display_name: "Fix Bug"
  short_description: "Evidence-first bug diagnosis and regression-safe repair"
  default_prompt: "Use $fix-bug to reproduce, diagnose, minimally repair, and verify this bug."
policy:
  allow_implicit_invocation: true
