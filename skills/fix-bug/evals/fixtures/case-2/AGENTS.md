# Project instructions

- Treat `PRD.md` and `EDD.md` as authoritative.
- Inspect Git history for reported regressions.
- Run tests with `conda run -n base python -m unittest discover -s tests -v`.
- Keep public APIs stable.
