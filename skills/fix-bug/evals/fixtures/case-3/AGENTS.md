# Project instructions

- `PRD.md` and `EDD.md` define the contract.
- Run tests with `conda run -n base python -m unittest discover -s tests -v`.
- A failing assertion is not, by itself, authority to change product behavior.
