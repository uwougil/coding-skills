# Project instructions

- Treat `PRD.md` as the product contract and `EDD.md` as the implementation contract.
- Run tests with `conda run -n base python -m unittest discover -s tests -v`.
- Do not edit contract files during a bug repair.
