# Project instructions

- `PRD.md` and `EDD.md` define range semantics.
- Run tests with `conda run -n base python -m unittest discover -s tests -v`.
- Tests that protect documented behavior must remain enabled.
